% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{prepare_barcode_data}
\alias{prepare_barcode_data}
\title{Prepare a vector of barcodes from a raw data frame}
\usage{
prepare_barcode_data(
  data,
  columns = barcode_columns,
  drop_primer_segment = FALSE,
  unique_only = TRUE,
  sep = "_",
  context = "prepare_barcode_data()"
)
}
\arguments{
\item{data}{Data frame read from the Excel workbook.}

\item{columns}{Character vector of columns to concatenate.}

\item{drop_primer_segment}{Logical; if `TRUE`, keep only the first segment of
each barcode (used when primers must be removed).}

\item{unique_only}{Logical; deduplicate barcodes when `TRUE`.}

\item{sep}{Separator used when concatenating the columns.}

\item{context}{Label describing the caller for diagnostic messages.}
}
\value{
Character vector of barcodes.
}
\description{
Selects the expected columns, concatenates them with an underscore, and
optionally strips primer information before returning a (optionally unique)
character vector of barcodes.
}
\keyword{internal}
