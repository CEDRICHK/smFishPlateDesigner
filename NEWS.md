# smFishPlateDesigner 0.1.1

- Switch Excel exports to `openxlsx` (no Java dependency) and add robust helper `write_plate_workbook()`.
- Refactor fish/dosage plate generation with shared helpers, validation checks, and improved control annotations.
- Add pkgdown configuration, vignette relocation, and GitHub Actions workflow to build documentation.
- Provide helper `probes_final_columns()` describing the sample Excel structure.
